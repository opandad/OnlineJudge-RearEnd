这个文件夹主要存放功能

```
-- contests 竞赛相关
    -- list 竞赛列表读取 (dev)
    -- detail 竞赛进入后的界面读取 (dev)(需检查权限)
    -- rank 排名
-- problems 问题相关
    -- list 问题列表读取 (test)
    -- detail 问题详细列表 （dev）
    -- insert_problem (dev)(需检查权限)
    -- update_problem (dev)(需检查权限)
    -- delete_problem (dev)(需检查权限)
        --one
        --more
-- users 用户相关
    -- forget_password 用户忘记密码相关 (dev)
    -- login 用户登录相关 (test)
    -- regist 用户注册相关 (test)
-- judger 判题机相关
```